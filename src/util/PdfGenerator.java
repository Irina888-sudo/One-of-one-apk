package util;

/**
 * PdfGenerator disabled placeholder.
 *
 * The salary invoice/export feature was removed at the user's request. This class remains
 * as a non-functional placeholder to avoid missing-class errors in older code.
 */
public class PdfGenerator {
    // invoice generation disabled
}
